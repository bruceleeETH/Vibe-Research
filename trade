#!/bin/sh
set -eu

ROOT=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
PYTHON="$ROOT/backend/.venv/bin/python"
if [ ! -x "$PYTHON" ]; then
  PYTHON=python3
fi

exec "$PYTHON" "$ROOT/tools/trade.py" "$@"
