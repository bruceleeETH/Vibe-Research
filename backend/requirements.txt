# 核心（轻量、秒装）——行情 + 研报即刻可用
fastapi>=0.110
uvicorn[standard]>=0.29
requests

# 全栈数据源（较重）——一致预期 / 新闻 / 公告 / K线 / 财务
# 未安装时对应端点会返回 501 + 安装提示，不影响行情/研报
akshare>=1.10
mootdx>=0.10
pandas

# 本地涨停研究的数值计算
numpy>=1.24

# 本地主板历史数据仓：列式分析库 + 历史日线/ST/停牌/复权
duckdb>=1.5,<2
baostock>=0.9.3,<1
