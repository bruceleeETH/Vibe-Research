# 简报自动更新验收

用户已授权完成自动更新验收。验收对象是现有早晚简报的定时读取、本地归档和工作台显示；不生成新简报，不改动持仓或成交。

## 2026-09-10 06:05 阶段结果

人工链路已通过，定时触发尚未验收，不能宣称整体通过。

- 发现并修复：同步说明使用的读取长度超过工具实际允许上限，会使自动任务直接失败。现使用 16000，截断时最多重试 20000。
- 发现并修复：凌晨手动检查可掩盖同日早间定时漏跑。现按每个同步时点判断，允许 10 分钟完成窗口，初次配置前不追认漏跑，跨午夜保留逾期状态。
- 实际读取两档来源：35 篇早报、7 篇晚报，最新均为 9 月 9 日；补齐本地历史，共 42 篇。第二次导入新增为零，42 篇原文哈希保持一致。
- 隔离目录模拟读取失败并恢复：原文保留、错误状态显示、恢复后错误清除，42 篇哈希不变。
- 72 项相关后端回归通过，前端构建通过。正式来源在 06:05 尚无 9 月 10 日内容是正常等待，不能算上游故障。

人工证据保存在忽略目录 `data/brief-acceptance/2026-09-10/`。这些文件不得当作定时执行证据。

## 实际触发与正式时段验收

在同一个 heartbeat 上增加临时 06:15 验收时点，保留原四个正常时点。下一次由调度器唤醒时按以下步骤执行，不要在人工发起的本轮伪造执行结果：

1. 先读忽略目录内 `original-automation.json`，用 automation_update 工具把频率恢复为原规则；保留当前包含本验收流程的提示词，直到正式早晚两档均通过。不得直接编辑 automation.toml，不新建 cron 或重复任务。若首次唤醒已过 06:15，也先恢复，再如实记录延迟。
2. 读取本文件和 `tools/brief_sync_instructions.md`，按修正参数实际同步两档；正常时段至少同步对应档。需要私有归档写入权限时，可按本任务授权申请执行权限，不因沙箱路径限制跳过导入。
3. 在 `data/brief-acceptance/2026-09-10/` 写本次独立 JSON 证据：实际起止时间、触发类型与该自动运行任务的证据、处理档位、读取是否成功、导入计数、原文哈希核对结果、最后检查时间、归档日期、API/页面对照结果。不要写令牌或整段正文。失败如实记录，并保留已有正文。
4. 用本机工作台 `/api/workbench/briefs` 或浏览器页面核对日期和正文是否对应、最后检查时间是否更新，读取失败时不能用旧页面证明成功。不要操作持仓页面或买卖接口。
5. 临时触发成功，只能报告“定时唤醒与同步链路通过”。完整验收还需正式早间、晚间各一轮：对应日期的新简报实际出现并自动归档，正文哈希一致，页面时间和状态一致；没有新内容或运行延迟则保留待验原因，继续后续正常时点。缺失的上游内容不伪造。
6. 正式两档均通过后，更新本文件的结果，保存证据，然后通过 automation_update 恢复 original-automation.json 中的原提示词和规则，保持自动同步启用；给用户一次完成通知。未完成前只在阶段完成、新故障或需要操作时通知，不重复播报相同等待状态。

如遇调度器未唤醒，保持未通过；若只是本机休眠/关闭，只能记录运行条件不满足，不能判定为数据源故障或用人工检查替代。
