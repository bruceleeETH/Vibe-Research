# 本地趋势策略验证首版

用户已要求继续并在本地看到运行效果。首版采用现有 React 工作台页面加公开行情快照文件；不增加交易接口，也不依赖私有持仓。相比独立临时 HTML，此方案保留工作台入口；相比全市场后台任务，先能完整核对样本与逐笔计算。

范围：截图中的 14 只主板股票加远东股份，共 15 只当前观察池。读取近一年及指标预热期前复权日线，研究 5/10 日上涨与放量条件。明确事后选池偏差和成交额数据缺口，不宣称全市场回测。

交付：可重复采集的 Python 命令；可复用的纯函数策略计算；工作台页面提供量能门槛、风险线、观察期、成本调整及重算，展示最新候选、无放量对照、逐信号结果、单股价格图和失败原因。基准版本只做固定止损与 D3 收盘到期退出，不冒称完成用户分批止盈与均线延续版本。

执行：收盘日 t 以收盘价为统一买入基准，D1=t+1 为第一观察/可卖日，继续观察至 D3。买入日低点不触发止损；隔日跳空按开盘基准，日内触线按止损价基准另扣成本；一字行情保守判为不可成交并延迟/记待定。止损是模型参考价不是保证成交。全部回测都要求到期日期已可观察，近期信号单独标待观察。显示成交额历史筛选尚未通过，不编造资金流入或板块实时热度。

验证：构造样本测试信号不能用未来量、T+1、跳空、止损后反弹仍按退出计、未成熟样本、不可成交行情；TypeScript 构建；浏览器真实点击重算、筛选与图表。保留用户已有 docs/plans/2026-09-10-brief-auto-acceptance.md 修改，不提交或推送。

## 完成与验收

### 日期对照改进

用户要求能自行选日期，看隔日开盘、最低、最高、均价、收盘和放量情况。新增页面首屏日期观察区，整年策略参数及汇总折叠到下方；默认最近有一个完整后续行情日的选股日期。支持日历选择、前后交易日、条件命中过滤及股票过滤。明确①当日收盘价作为买入基准、②隔日为第一观察/可卖日；各日价格与放量分开计算，选股筛选不得使用后续量能。缺失行情或未来日期显示为空，不顺延成其他日期。单股明细连续展示买入日至 D3 的开低高均价收盘、成交量和放量倍数。使用已有前复权日线，百分比相对买入日收盘，未扣费或模拟退出，不标成实际利润。

- 15/15 股票读取成功，数据截至 2026-09-11，采集时刻北京时间 16:52:35。近一年研究范围 2025-09-11 至 2026-09-11。
- 新增本地入口 http://127.0.0.1:5899/investment-workbench/trend-lab；正在运行的服务来自主目录，因此将新增文件和两条导航改动同步到 /Users/li/Documents/GitHub/710VibeResearch/Vibe-Research。该目录原有未跟踪券商 PDF 保留。
- 默认 1.5 倍量、5% 风险线、D3 到期、往返成本 0.2%：317 个完整信号，净正收益 43.8486%，平均净收益 0.6931%，最差 -14.2015%；另外 25 个待观察、2 个保守未成交。无放量对照 1493 个完整信号，净正收益 42.8667%，平均 0.5301%。这是重叠样本描述统计，不代表显著优势。
- 18 项纯函数测试全部通过；npm run build 通过；git diff --check 通过。构建存在项目大包体提醒，不影响完成。
- 浏览器访问首次因原授权仅限每日简报被拒绝；用户随后明确允许趋势页的查看、重算与截图。实际确认 1.5 → 2 倍后样本 317 → 123，最新条件命中 7 → 3；恢复默认后核对历史结果，远东股份过滤显示 22 个信号。页面已截图检查并保留为交付标签。
- 首版有待扩展：全主板历史股票池、真实成交额与板块热度、历史状态、分批退出/趋势延续、每日跟踪。页面重读按钮只读取本地行情，不伪装为网络采集。当前研究明确采用收盘价基准，不要求额外采集14:40快照。
